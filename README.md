# Custom_LSTM
My simple custom LSTM NN for last phoneme predictions in a sequences of sounds.

Instructions:
1) Requires .seg_Y1 and corresponding .seg_B1 files for training;
2) Download the repository;
3) Create virtual environment and install requirements.txt;
4) Launch main.py.
